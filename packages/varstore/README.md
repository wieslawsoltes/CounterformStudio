# OpenType item variation stores and index maps

Standalone ES modules. No native, DOM or network dependency. See the exported declarations and repository tests for contracts.

The public avar 1.0 codec normalizes F2DOT14 map pairs, validates monotonicity/endpoints, interpolates mapped coordinates and encodes/decodes bounded table data. avar 2.0 is rejected.
