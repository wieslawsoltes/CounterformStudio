import http from 'node:http';import fs from 'node:fs/promises';import path from 'node:path';import {fileURLToPath}from'node:url';import {writeIndex}from'./importmap.mjs';
const project=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),root=path.resolve(process.env.CF_ROOT||project);if(root===project)await writeIndex(root);const port=Number(process.env.PORT||4173),types={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.mjs':'text/javascript; charset=utf-8','.json':'application/json','.css':'text/css; charset=utf-8','.wasm':'application/wasm','.svg':'image/svg+xml','.png':'image/png','.map':'application/json','.md':'text/plain; charset=utf-8'};
const server=http.createServer(async(req,res)=>{try{const name=decodeURIComponent(new URL(req.url,'http://localhost').pathname),filename=path.resolve(root,'.'+name+(name.endsWith('/')?'index.html':''));if(!filename.startsWith(root+path.sep)){res.writeHead(403).end();return;}const real=await fs.realpath(filename);if(!real.startsWith(root+path.sep)||real.includes('/node_modules/')){res.writeHead(403).end();return;}const content=await fs.readFile(real);res.writeHead(200,{'Content-Type':types[path.extname(real)]||'application/octet-stream','Cross-Origin-Opener-Policy':'same-origin','Cross-Origin-Embedder-Policy':'require-corp','Cross-Origin-Resource-Policy':'same-origin','X-Content-Type-Options':'nosniff','Cache-Control':'no-store'});res.end(content);}catch(error){res.writeHead(error.code==='ENOENT'?404:400,{'Content-Type':'text/plain'});res.end('Resource unavailable');}});server.listen(port,'127.0.0.1',()=>console.log(`Counterform Studio: http://127.0.0.1:${port}/`));for(const sig of ['SIGINT','SIGTERM'])process.on(sig,()=>server.close(()=>process.exit()));
