# 0.6.0 source delivery

The complete editable implementation was committed directly to `main` at `0bc022bf856f5b4a9ead4f96fe3ccca50f019ded`, preserving the verified 0.5.1 baseline `672fede334bb2bc42d1ec4be59c18cc40a643269` and all upstream library files. The source-materialization workflow `35491486536` verified the delivery SHA-256, exact changed-path inventory, and all 94 before/after Git blob identities before passing all 167 core tests and committing the source.

This release implements explicit GPOS attachment authoring/compilation (cursive, mark-to-base, mark-to-ligature, mark-to-mark), mark filtering/attachment classes, avar 1.0 axis mapping, bounded outline measurements and curvature analysis, and their transactional workbench editors. See [release notes](RELEASE-0.6.0.md) and [authoring/API documentation](ADVANCED-AUTHORING.md) for contracts, algorithms, and remaining exclusions. This is not a claim of exhaustive FontLab parity.

Local qualification passed 167 core tests, five native-readiness tests, strict TypeScript consumers, 63 independent font/layout checks, and all 30 extracted npm consumers including actual Node workers. Isolated local source/distribution browser checks passed 11 new authoring tests, nine input/lifetime tests, 57 original editor tests with three explicit secure-origin skips, and 14 workspace tests with one explicit preference-storage skip on each build. The local browser's HTTP navigation policy required isolated assets and explicit inline compilation; those results do not certify browser workers or secure storage.

The one-shot materialization workflow has been retired. Normal CI now includes the new 11-check authoring suite on both source and distribution, with real browser workers, alongside all retained editor, workspace, storage, font, typed and package gates. Its deployment and live-pages jobs qualify the exact deployed commit separately. Consult the commit-associated run for the current outcome; this source receipt does not claim that pending jobs have completed. Software-rendered Chromium is not physical WebGPU, Safari/Firefox or assistive-technology certification. Packages are packed and consumer-tested, not automatically published to npm.
